{
  "name": "contextforge",
  "version": "0.1.0",
  "private": true,
  "description": "A TypeScript RAG and agent-orchestration reference project",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "serve": "npm run build && node dist/http/server.js",
    "test": "npm run build && node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.6.2"
  }
}
